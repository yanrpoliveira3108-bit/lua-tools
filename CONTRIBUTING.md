# Contribuindo para Lua Tools - yna.019

Obrigado por querer contribuir!

## Como contribuir

1. Fork o repositório
2. Crie branch: `git checkout -b feature/minha-feature`
3. Commit: `git commit -m "feat: minha feature"`
4. Push: `git push origin feature/minha-feature`
5. Abra Pull Request

## Regras

- Mantenha marca d'água `yna.019` em embeds principais (anti-roubo)
- Use `encoding="utf-8"` ao abrir `config.json` (fix Windows)
- Teste `python -m py_compile cogs/*.py`
- Adicione comandos em `docs/COMANDOS.md`
- Modulos: adicione `check_modulo()` e registro em `config["modulos"]`

## Código de conduta

Seja respeitoso. Dev oficial: yna.019 | Insta: yna.019 | DC: yna.019
